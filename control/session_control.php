<?php

//START SESSION
//session_name('negotiatus_login');

//SET COOKIE FOR 1 WEEK LIFE
//session_set_cookie_params(1*7*24*60*60,'/');

//session_start();

//if($_COOKIE['id'] && !isset($_COOKIE['negotiatus_remember'])) {
    //If you are logged in but do not have 'negotiatus_remember' (browser restart)
    //and have not checked remember me box;
    
    //$_SESSION = array();
   // session_destroy();
    
    
//}

//if(isset($_GET['logoff'])) {
    //setcookie ("negotiatus_remember", "", time()-60000,'/'); 
    //setcookie ("username", "", time()-60000,'/');
    //setcookie ("id", "", time()-60000,'/');
    
    //session_destroy();
    
//}

?>